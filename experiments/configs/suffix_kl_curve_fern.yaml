experiment_name: suffix_kl_curve_fern
output_user: dani

n_sequences: 1000
seq_length: 500
k_max: 25
n_positions_per_seq: 50

# Optional: overlay the LLM's empirical KL as a horizontal reference line.
# Set to null to omit, or fill in from existing experiment results.
model_empirical_kl: null

random_seed: 42

hmm:
  process_name: fern
  process_params:
    x: 0.63

# No model is loaded; this field is required by ExperimentConfig but unused.
model_name: "n/a"
